Entities:
game object
    <light source?>
    model
        draw()
        positional data.
        meshs - To render it, need a material. (shader and textures)
            construct with
                path name and shader
                material
            vertex data(done)
            material(done)
                shader (done)
                textures (done)
